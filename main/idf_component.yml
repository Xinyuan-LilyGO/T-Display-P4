dependencies:
  cpp_bus_driver:
    override_path: "../libraries/cpp_bus_driver"

  lilygo_device_driver:
    override_path: "../libraries/lilygo_device_driver"

  # private_library:
  #   override_path: "../libraries/private_library"
  #   matches:
  #     - if: "$CONFIG{EXAMPLE_BUILD_ES8311} == True"
  #     - if: "$CONFIG{EXAMPLE_BUILD_RADIOLIB_LR1121_SEND_RECEIVE} == True"

  espressif/esp-sr:
    version: "^2.1.3"
    matches:
      - if: "$CONFIG{EXAMPLE_BUILD_AFE} == True"

  # lvgl/lvgl:
  #   version: "9.4.0"

  # espressif/esp_tinyusb:
  #   version: "^1"
  #   matches:
  #     - if: "$CONFIG{ENABLE_USB_DISPLAY} == False"



  # espressif/esp_hosted:
  #   version: "2.0.17"

  # espressif/esp_wifi_remote:
  #   version: "^1.1"

  # espressif/esp_lcd_usb_display:
  #   version: "^1.0.0"
  #   matches:
  #     - if: "$CONFIG{ENABLE_USB_DISPLAY} == True"

  # espressif/ethernet_init:
  #   version: "^0.7.0"
